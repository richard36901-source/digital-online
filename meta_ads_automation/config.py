"""
תצורת האוטומציה - ערוך את הקובץ הזה לפני ההרצה הראשונה.
"""

import os

# ==== System User Access Token ====
# העתק לכאן את הטוקן מתוך meta_api_credentials.txt
# עדיף להגדיר כמשתנה סביבה (META_ACCESS_TOKEN) במקום לשים כאן בטקסט גלוי.
ACCESS_TOKEN = os.environ.get("META_ACCESS_TOKEN", "PASTE_YOUR_TOKEN_HERE")

# גרסת ה-API
API_VERSION = "v21.0"
GRAPH_URL = f"https://graph.facebook.com/{API_VERSION}"

# ==== חשבונות הלקוחות ====
# מפתח = שם ידידותי לזיהוי בלוגים, ערך = מזהה חשבון המודעות (בלי act_)
AD_ACCOUNTS = {
    "רחל": "1362031111389290",
    "טל": "2351574495311508",
    "גל": "1703717016873346",
}

# ==== דפי הפייסבוק של כל לקוח ====
# נמצאו אוטומטית מתוך שדה "זהות" במודעות קיימות בכל חשבון (06/08/2026).
# משמשים ליצירת קריאטיבים חדשים בזמן רוטציה.
PAGE_IDS = {
    "רחל": "464521926734949",   # הכנסה דיגיטלית
    "טל": "1148192765034871",   # הכנסה בלחיצה
    "גל": "809936088879584",    # הכנסה בעידן הדיגיטלי
}

# ==== כללי האוטומציה ====
# סף רווחיות - מתחת לזה, המודעה מסומנת כלא רווחית ומושהית
ROAS_THRESHOLD = 1.5

# כמה ימים מודעה צריכה לרוץ לפני שהיא מועמדת לרוטציית קריאטיב
CREATIVE_ROTATION_DAYS = 3

# חלון הנתונים לבדיקת ביצועים (Meta date_preset)
DATE_PRESET = "last_7d"

# ==== ספריית קריאטיבים ====
# תיקייה מקומית עם תמונות/וידאו + קובץ copy.json לטקסטים.
# אם עובדים מול גוגל דרייב - מומלץ לסנכרן את התיקייה מקומית (Google Drive Desktop)
# ולהצביע לכאן את הנתיב המקומי.
CREATIVE_BANK_PATH = "./creative_bank"

# ==== מצב בטיחות ====
# כשזה True, הסקריפט רק מדפיס/רושם מה הוא *היה* עושה, בלי לבצע שינויים אמיתיים.
# יש להריץ כמה ימים במצב הזה ולוודא שההחלטות הגיוניות, ואז לשנות ל-False.
DRY_RUN = True

# ==== לוגים ====
LOG_FILE = "./logs/automation_log.jsonl"

# ==== חשבונות לדשבורד (god_manager) ====
# כולל את כל הלקוחות + את החשבון האישי שלך (naan1930store).
# נפרד מ-AD_ACCOUNTS בכוונה: הדשבורד קריאה-בלבד (לא נוגע/משהה/מחליף כלום),
# ולכן בטוח לכלול בו גם את החשבון האישי בלי להרחיב את היקף האוטומציה שמבצעת פעולות.
DASHBOARD_ACCOUNTS = {
    **AD_ACCOUNTS,
    "שלי (naan1930store)": "330184635273905",
}

# קובץ הדשבורד שנוצר מחדש בכל הרצה
DASHBOARD_FILE = "./god_manager.html"
